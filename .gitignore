.zig-cache/
zig-out/

# Local agent config and worktrees
.claude/

# Local reference notes
ref/

# Fixtures that write into the repo root at test time.
# ch09_system_tasks/s01_13_long_record_is_not_truncated.va $fopen()s these "w"
# and then reads them back; they are outputs, not inputs.
/s01_13_built.dat
/s01_13_wide.dat
