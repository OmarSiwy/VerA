module ch09_atomic_rewind(p, n);
  inout p, n; electrical p, n;
  integer fd, code;
  analog begin
    fd = $fopen("fixture.dat", "r");
    code = $rewind(fd);
    I(p,n) <+ V(p,n) + code;
  end
endmodule

